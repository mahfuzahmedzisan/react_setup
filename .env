# API (required). Vite only exposes vars prefixed with VITE_
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Laravel Passport JSON — Bearer token (not HttpOnly cookie)
VITE_AUTH_STRATEGY=bearer_memory

# Where to store the Bearer token: session (default) | local | memory
# session = survives Ctrl+Shift+R in this tab; local = all tabs until logout; memory = never survives reload
VITE_BEARER_TOKEN_STORAGE=session

VITE_AUTH_ME_PATH=/me

# VITE_AUTH_LOGOUT_PATH=/logout
